% ============================================================
%  Python dla inżynierów .NET
%  Czytanie, pisanie i wdrażanie Pythona, kiedy C# jest twoim pierwszym językiem
%  Konrad Cinkusz
%
%  POLISH edition. A4, 12pt, one-sided: this book is read on a screen with an
%  editor open beside it, and is not laid out for print.
%
%  Two main files exist, one per language. They differ only in the two lines
%  below -- \booklang and the PDF title. Everything else, including the whole
%  document body, is shared, so a chapter cannot go missing from one of them.
%
%  Build:  latexmk -pdf main-pl.tex        (or: make pl)
%
%  To work on one chapter only, uncomment \includeonly.
% ============================================================

\documentclass[12pt,oneside,openany]{book}

\def\booklang{pl}
\input{preamble}

\hypersetup{pdftitle={Python dla inżynierów .NET}}

% \includeonly{chapters/pl/ch01-runtime}

\input{body}
