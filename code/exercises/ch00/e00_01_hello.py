"""Exercise 0.1 -- the loop this book is read through.

Open this file in the editor beside the PDF. The test next to it,
test_e00_01_hello.py, fails until `greet` returns the string the test
expects. Make it pass, then run the test again with -v and read what pytest
printed.
"""


def greet(name: str) -> str:
    """Return the greeting test_e00_01_hello.py expects for `name`."""
    raise NotImplementedError("your turn: replace this line")
